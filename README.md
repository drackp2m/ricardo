# Ricardo's Clock In

### Development
You can execute `python3 serve.py --dir src --base /ricardo`.
Additionally you can pass the --port argument, the default is 8000

### Demo url
Visit on https://drackp2m.github.io/ricardo
